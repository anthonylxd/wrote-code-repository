<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.inter.hostinter">
<select id="hostlogin" parameterType="host" resultType="host">
	select * from host where username=#{username} and password=#{password}
</select>
</mapper>