<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dao.userDao">

<select id="userLogin" parameterType="com.entity.user" resultType="user">
select * from user where username = #{username} and password=#{password}
</select>

<insert id="register" parameterType="com.entity.user" useGeneratedKeys="true" keyProperty="userId">
insert into user (username,password) values(#{username},#{password})
</insert>

<select id="userSelect" parameterType="string" resultType="user">
select userId,username from user where username like %#{_parameter}%
</select>

<select id="userCheck" parameterType="string" resultType="int">
select count(*) from user where username = #{_parameter}
</select>

<select id="usernameSelectById" parameterType="int" resultType="string">
select username from user where userId = #{_parameter}
</select>

<select id="userSelectById" parameterType="int" resultType="user">
select userId,username from user where userId = #{_parameter}
</select>

<select id="userSelectByUsername" parameterType="string" resultType="user">
select userId,username from user where username = #{_parameter}
</select>

<update id="passwordUpdate" parameterType="string">
update user set password = #{0} where userId = #{1}
</update>
<!--  
<select id="displayuser" resultType="user">
select * from user
</select>
<delete id="deleteuser" parameterType="string">
delete from user where username =#{_parameter}
</delete>-->
</mapper>