<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dao.relationDao">

<insert id="relationInsert" parameterType="relation">
	insert into relation (followUserId,fanUserId) values (#{followUserId},#{fanUserId})
</insert>

<select id="relationSelect" parameterType="int" resultType="java.lang.Integer">
	select followUserId from relation where fanUserId = #{_parameter}
</select>

<select id="relationIsSelect" parameterType="int" resultType="int">
	select count(*) from relation where followUserId = #{0} and fanUserId = #{1}
</select>

<select id="relationSelectByfollowUserId" parameterType="int" resultType="int">
	select fanUserId from relation where followUserId = #{_parameter}
</select>

<delete id="relationDelete" parameterType="relation">
	delete from relation where followUserId = #{followUserId} and fanUserId = #{fanUserId}
</delete>

</mapper>